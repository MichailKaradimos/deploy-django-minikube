secretGenerator:
# These secrets are generated by Kubernetes and
# can be used to store sensitive data like passwords or API keys.
  - name: db-password
    literals:
      - password=supersecurepasswordhere
  - name: django
    literals:
      - secret=secretkey123
resources:
#  These resources can include things like 
# deployments, services, and persistent volumes.
  - postgres.yaml
  - django.yaml